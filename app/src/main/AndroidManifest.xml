<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.VelouReservation"
        tools:targetApi="31">

        <activity
            android:name="com.stripe.android.payments.StripeBrowserLauncherActivity"
            android:exported="false"
            tools:replace="android:exported">
        </activity>

        <!-- MainActivity declaration -->
        <activity
            android:name=".UI.MainActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ValidActivity declaration -->
        <activity
            android:name=".UI.ValidActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Specify your custom success URL scheme -->
                <data android:scheme="myapp" android:host="success" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.EchecActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Specify your custom failure URL scheme -->
                <data android:scheme="myapp" android:host="cancel" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.PaylaterActivity"
            android:exported="true">
            <!-- Add any additional intent filters or configuration for ValidActivity here if needed -->
        </activity>

        <activity
            android:name=".UI.PromocodeActivity"
            android:exported="true">
            <!-- Add any additional intent filters or configuration for ValidActivity here if needed -->
        </activity>
        <activity
            android:name=".UI.CardActivity"
            android:exported="true">
            <!-- Add any additional intent filters or configuration for CardActivity here if needed -->
        </activity>

    </application>

</manifest>
